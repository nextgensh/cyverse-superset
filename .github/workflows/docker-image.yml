name: cyverse-superset

on:
  push:
    branches: [ "root-only" ]
  pull_request:
    branches: [ "root-only" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
